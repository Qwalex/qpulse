# Local development
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Railway production
# Set NEXT_PUBLIC_API_URL as a Railway variable BEFORE the first build.
# It is baked into the client bundle at build time.
# NEXT_PUBLIC_API_URL=https://your-api.up.railway.app/api/v1
# NODE_ENV=production
